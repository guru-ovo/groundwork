# Featherless.ai — OpenAI-compatible endpoint
FEATHERLESS_API_KEY=your_key_here
FEATHERLESS_BASE_URL=https://api.featherless.ai/v1

# Small/cheap model for occupation-title matching (Stage 0).
# $0.04/$0.15 per 1M tokens, 131k context, and — critically — NOT gated.
#
# Both meta-llama/*Llama-3.1-8B-Instruct slugs are cheaper on paper but are
# HuggingFace-gated: they need an OAuth link per organisation and otherwise
# fail with model_gated_needs_oauth. Check `is_gated` in
# https://api.featherless.ai/v1/models before switching to any model.
FEATHERLESS_SMALL_MODEL=openai/gpt-oss-20b

# Strong model for roadmap synthesis (Stage 4). $0.10/$0.55 per 1M, 131k ctx.
# moonshotai/Kimi-K3 also works but costs ~18x more per call for a task that
# is a constrained JSON synthesis over an already-grounded summary.
FEATHERLESS_STRONG_MODEL=openai/gpt-oss-120b

# O*NET Web Services — register free at https://services.onetcenter.org/
ONET_USERNAME=your_onet_username
ONET_PASSWORD=your_onet_password

# CORS — comma-separated exact origins allowed to call this API.
# Defaults to the local Vite dev server if unset. On Render, set this to your
# Vercel production URL. Vercel *.vercel.app preview deploys are already
# allowed by regex in app/main.py.
ALLOWED_ORIGINS=http://localhost:5173
